# Ejemplo de archivo .env para configurar Web Scraper PRO
# Copia este archivo a .env y modifica los valores según sea necesario.

# --- Configuración del Crawler ---
# Número de trabajadores concurrentes
CONCURRENCY=10

# --- Configuración de la Base de Datos ---
# Ruta al archivo de la base de datos
DB_PATH="data/my_database.db"

# --- Configuración de LLM ---
# Clave API para el Modelo de Lenguaje Grande (ej. OpenAI)
LLM_API_KEY="sk-..."

# --- Configuración de Pre-calificación de URL (Opcional) ---
# Habilitar pre-calificación con peticiones HEAD para descartar URLs no deseadas rápidamente.
PREQUALIFICATION_ENABLED=true

# Tipos de contenido permitidos, separados por comas (ej. "text/html,application/xhtml+xml").
ALLOWED_CONTENT_TYPES="text/html,application/xhtml+xml,application/xml"

# Tamaño máximo de contenido en bytes para descargar (ej. 10MB = 10000000).
MAX_CONTENT_LENGTH_BYTES=10000000
